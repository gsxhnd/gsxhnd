---
import Layout from "../../layouts/Layout.astro";
---

<Layout title="EC平台重新设计 | CName Inc.">
  <section class="work-detail">
    <div class="work-detail__container">
      <article class="work-article">
        <header class="work-article__header">
          <h1>EC平台重新设计</h1>
          <div class="work-meta">
            <span class="work-meta__tag">Web网站・应用开发</span>
            <span class="work-meta__category">时尚品牌</span>
          </div>
        </header>

        <div class="work-article__content">
          <h2>项目概览</h2>
          <p>
            为大型时尚零售商完全重新设计了在线商店。我们整理了复杂的界面，基于用户中心的设计原则建立了新平台。
          </p>

          <h2>挑战</h2>
          <ul>
            <li>复杂的导航结构使用户难以找到目标产品</li>
            <li>多步骤购买流程导致购物车放弃率高</li>
            <li>响应式设计不完善，移动端体验差</li>
            <li>页面加载速度慢，用户流失多</li>
          </ul>

          <h2>解决方案</h2>
          <p>通过以下方法大幅改善了用户体验：</p>
          <ul>
            <li>基于用户研究的简洁直观UI设计</li>
            <li>简化购买流程（单步结账）</li>
            <li>移动优先的响应式设计</li>
            <li>通过图像优化和延迟加载改善性能</li>
            <li>通过A/B测试进行持续改进</li>
          </ul>

          <h2>成果</h2>
          <p>项目完成后取得了以下成果：</p>
          <ul>
            <li>转化率提高 40%</li>
            <li>购物车放弃率降低 25%</li>
            <li>页面加载速度提高 60%</li>
            <li>移动用户数增加 80%</li>
          </ul>
        </div>

        <div class="work-article__footer">
          <a href="/works/" class="work-back-link">← 返回作品列表</a>
        </div>
      </article>
    </div>
  </section>
</Layout>

<style lang="scss">
  .work-detail {
    padding: 2rem 1.5rem;
    background: var(--c-bg-secondary);

    @media (min-width: 768px) {
      padding: 3rem 2rem;
    }

    &__container {
      max-width: 800px;
      margin: 0 auto;
    }
  }

  .work-article {
    background: white;
    padding: 1.5rem;
    border-radius: 8px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);

    @media (min-width: 768px) {
      padding: 3rem;
    }

    &__header {
      margin-bottom: 1.5rem;
      padding-bottom: 1.5rem;
      border-bottom: 1px solid var(--c-border-light);

      @media (min-width: 768px) {
        margin-bottom: 2rem;
        padding-bottom: 2rem;
      }

      h1 {
        font-size: 1.5rem;
        margin-bottom: 0.8rem;
        color: var(--c-text);

        @media (min-width: 768px) {
          font-size: 2rem;
          margin-bottom: 1rem;
        }
      }
    }

    &__content {
      color: var(--c-text-sub);
      line-height: 1.8;

      p {
        margin-bottom: 1rem;
        font-size: 0.9rem;

        @media (min-width: 768px) {
          margin-bottom: 1.5rem;
          font-size: 1rem;
        }
      }

      h2 {
        font-size: 1.2rem;
        margin: 1.5rem 0 0.8rem;
        color: var(--c-text);

        @media (min-width: 768px) {
          font-size: 1.5rem;
          margin: 2rem 0 1rem;
        }
      }

      ul {
        margin-bottom: 1rem;
        margin-left: 1.2rem;

        @media (min-width: 768px) {
          margin-bottom: 1.5rem;
          margin-left: 1.5rem;
        }

        li {
          margin-bottom: 0.4rem;
          font-size: 0.9rem;

          @media (min-width: 768px) {
            margin-bottom: 0.5rem;
            font-size: 1rem;
          }
        }
      }

      strong {
        color: var(--c-text);
        font-weight: 600;
      }
    }

    &__footer {
      margin-top: 1.5rem;
      padding-top: 1.5rem;
      border-top: 1px solid var(--c-border-light);

      @media (min-width: 768px) {
        margin-top: 2rem;
        padding-top: 2rem;
      }
    }
  }

  .work-meta {
    display: flex;
    gap: 0.8rem;
    flex-wrap: wrap;

    @media (min-width: 768px) {
      gap: 1rem;
    }
  }

  .work-meta__tag {
    display: inline-block;
    background: #e8f0ff;
    color: var(--c-primary);
    padding: 0.3rem 0.8rem;
    border-radius: 4px;
    font-size: 0.75rem;
    font-weight: 500;

    @media (min-width: 768px) {
      padding: 0.4rem 1rem;
      font-size: 0.85rem;
    }
  }

  .work-meta__category {
    display: inline-block;
    background: var(--c-bg);
    color: var(--c-text-sub);
    padding: 0.3rem 0.8rem;
    border-radius: 4px;
    font-size: 0.75rem;
    border: 1px solid var(--c-border);

    @media (min-width: 768px) {
      padding: 0.4rem 1rem;
      font-size: 0.85rem;
    }
  }

  .work-back-link {
    color: var(--c-primary);
    text-decoration: none;
    font-weight: 600;
    transition: color 0.3s;
    font-size: 0.9rem;

    @media (min-width: 768px) {
      font-size: 1rem;
    }

    &:hover {
      color: var(--c-primary-light);
    }
  }
</style>
